{port_envs, [{"LDFLAGS", "$LDFLAGS -levent"}
             ]}.

{so_name, "bin/enet_tap"}.
{erl_opts, [debug_info]}.
{erl_first_files, [enet_codec]}.

% {plugins, [pc]}.

% {provider_hooks,
%  [
%   {pre,
%    [
%     {compile, {pc, compile}},
%     {clean, {pc, clean}}
%    ]
%   }
%  ]
% }.